apiVersion: v1
kind: Service
metadata:
  name: bot-service
  namespace: dcm-staging
  labels:
    app: dcm-bot
    environment: staging
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: dcm-bot
    environment: staging